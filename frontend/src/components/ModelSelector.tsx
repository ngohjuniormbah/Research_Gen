import {ChevronDown,RefreshCw,Server} from 'lucide-react'; import {useState} from 'react'; import type {BackendModel} from '@/types';
export function ModelSelector({models,value,onChange,loading,onRefresh,disabled}:{models:BackendModel[];value:string;onChange:(v:string)=>void;loading:boolean;onRefresh:()=>void;disabled?:boolean}){
 const [open,setOpen]=useState(false); const selected=models.find(x=>x.key===value);
 return <div className="relative"><button disabled={disabled||loading} onClick={()=>setOpen(!open)} className="flex items-center gap-2 rounded-xl border border-slate-700 bg-slate-900 px-3 py-2 text-sm hover:border-emerald-500/40 disabled:opacity-50"><Server size={15} className="text-emerald-400"/>{loading?'Chargement des modèles…':selected?.label||'Sélectionner un modèle'}<ChevronDown size={14}/></button>
 {open&&<div className="absolute right-0 top-11 z-30 w-80 overflow-hidden rounded-xl border border-slate-700 bg-slate-900 shadow-2xl"><div className="flex items-center justify-between border-b border-slate-800 px-3 py-2 text-[11px] text-slate-400"><span>Modèles disponibles</span><button onClick={onRefresh}><RefreshCw size={13}/></button></div>{models.map(m=><button key={m.key} onClick={()=>{onChange(m.key);setOpen(false)}} className={`block w-full px-3 py-2.5 text-left hover:bg-slate-800 ${m.key===value?'bg-slate-800':''}`}><div className="text-sm font-medium">{m.label||m.key}</div><div className="text-xs text-slate-400">{m.key} · {m.model} · {m.location||m.kind}</div></button>)}</div>}</div>
}
